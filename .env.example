# Copy to .env and fill in real values. Do NOT commit a populated .env.
# DockerHub image coordinates (image: ${DUSER}/${IMAGE})
DUSER=joakim077
IMAGE=springboot-application

# Database credentials — required by docker-compose.yml (no defaults are baked in).
MYSQL_ROOT_PASSWORD=change-me
SPRING_DATASOURCE_PASSWORD=change-me
