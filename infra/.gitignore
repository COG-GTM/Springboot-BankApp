# Terraform working artifacts (never commit state or provider binaries)
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfplan
tfplan.bin
tfplan.json
# Keep .terraform.lock.hcl committed for reproducible provider versions

# Sentinel CLI cache
policies/sentinel/.sentinel/
