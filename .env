DUSER=joakim077
IMAGE=springboot-application

# Database credentials are NOT stored in this file (it is committed to Git).
# Export them from your secret manager / Jenkins credentials before `docker compose up`:
#   export MYSQL_ROOT_PASSWORD=...        # administrative account
#   export SPRING_DATASOURCE_PASSWORD=... # least-privilege app account (default user: bankapp)
