# Security: secret values should be managed via an external secrets operator
# (e.g., AWS Secrets Manager, HashiCorp Vault, or Sealed Secrets) in production.
# The base64 values below are placeholders for local/dev environments only.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: bankapp-namespace
  labels:
    app: bankapp
type: Opaque
data:
  MYSQL_ROOT_PASSWORD: VGVzdEAxMjM=
  SPRING_DATASOURCE_PASSWORD: VGVzdEAxMjM=
